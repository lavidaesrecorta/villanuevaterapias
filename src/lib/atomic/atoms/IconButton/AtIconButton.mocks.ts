import { IAtIconButton } from './AtIconButton';

const base: IAtIconButton = {
  iconSrc: "/andrea/facebook.svg",
  iconTitle: "Facebook",
};

export const mockAtIconButtonProps = {
  base,
};